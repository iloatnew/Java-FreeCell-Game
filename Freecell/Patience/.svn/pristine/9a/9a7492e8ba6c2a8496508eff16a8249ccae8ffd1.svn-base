package model;

import javafx.scene.paint.Color;

public enum Suit {

	SPADE,DIAMOND,HEART,CLUB;

	/**
	 */
	public Color color() {
		return (this == SPADE || this == CLUB) ? Color.BLACK : Color.RED;
	}
	
}
